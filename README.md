# Course Registration System

University project for a course registration system.

- `backend/`: FastAPI backend (MVC-style)
- `frontend/`: will contain the web frontend (React/Vue/Angular), to be added later
- `docs/`: documentation (API, architecture, etc.)
