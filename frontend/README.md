# Frontend – Course Registration System

This folder will contain the frontend application (e.g., React/Vue/Angular).

The backend lives in `../backend` and exposes a JSON API that the frontend will consume.
